{
  "$schema": "./node_modules/wrangler/config-schema.json",
  "name": "chatwright-dev",
  "main": "worker/index.ts",
  "compatibility_date": "2026-07-21",
  // Install scripts (worker/install/*) are bundled as plain text so the
  // worker serves them byte-for-byte — no TS template-literal escaping.
  "rules": [{ "type": "Text", "globs": ["**/*.sh", "**/*.ps1"], "fallthrough": true }],
  "workers_dev": false,
  "routes": [
    {
      "pattern": "chatwright.dev",
      "custom_domain": true
    }
  ],
  "assets": {
    "directory": "./dist/chatwright-studio-prototype/browser",
    "binding": "ASSETS",
    "run_worker_first": true,
    "html_handling": "none",
    "not_found_handling": "single-page-application"
  }
}
